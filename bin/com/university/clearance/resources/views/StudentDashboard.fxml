<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<VBox xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1" 
      fx:controller="com.university.clearance.controller.StudentDashboardController"
      spacing="20" style="-fx-background-color: #f8f9fa;">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- Header Section -->
    <VBox spacing="10">
        <Label text="🎓 Student Clearance Dashboard" 
               style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
        <Label fx:id="lblWelcome" text="Welcome, Student" 
               style="-fx-font-size: 16px; -fx-text-fill: #7f8c8d;"/>
    </VBox>

    <!-- Status Message -->
    <VBox spacing="10">
        <Label text="Clearance Status:" style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
        <Label fx:id="lblStatusMessage" text="No clearance request submitted yet." 
               wrapText="true" style="-fx-font-size: 14px; -fx-padding: 10; -fx-background-color: #ecf0f1; -fx-background-radius: 5;"/>
    </VBox>

    <!-- Action Buttons -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <Button fx:id="btnSubmitRequest" text="🚀 Submit Clearance Request" 
                onAction="#submitClearanceRequest" 
                style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold; -fx-padding: 10 20;"
                prefWidth="250" prefHeight="40"/>
        
        <Button fx:id="btnRefresh" text="🔄 Refresh Status" 
                onAction="#refreshStatus"
                style="-fx-background-color: #95a5a6; -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold; -fx-padding: 10 20;"
                prefWidth="150" prefHeight="40"/>
    	<!-- Add this button near the other buttons in StudentDashboard.fxml -->
<Button text="📄 Generate Certificate" onAction="#generateCertificate" 
        style="-fx-background-color: #9b59b6; -fx-text-fill: white; -fx-font-weight: bold;"/>
    
    
    
    </HBox>

    <!-- Approval Progress Section -->
    <VBox spacing="15">
        <Label text="📊 Approval Progress" style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
        
        <TableView fx:id="tableApprovals" prefHeight="400" style="-fx-font-size: 14px;">
            <columns>
                <TableColumn fx:id="colDepartment" text="Department" prefWidth="200"/>
                <TableColumn fx:id="colOfficer" text="Officer" prefWidth="180"/>
                <TableColumn fx:id="colStatus" text="Status" prefWidth="120"/>
                <TableColumn fx:id="colRemarks" text="Remarks" prefWidth="200"/>
                <TableColumn fx:id="colDate" text="Approval Date" prefWidth="150"/>
            </columns>
            <columnResizePolicy>
                <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
            </columnResizePolicy>
        </TableView>
    </VBox>

    <!-- Information Section -->
    <VBox spacing="10" style="-fx-padding: 15; -fx-background-color: #e8f4f8; -fx-background-radius: 10;">
        <Label text="ℹ️ How the Clearance Process Works:" 
               style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #2980b9;"/>
        <VBox spacing="5">
            <Label text="• Submit your clearance request to start the process" 
                   style="-fx-text-fill: #2c3e50;"/>
            <Label text="• Six departments need to approve your clearance" 
                   style="-fx-text-fill: #2c3e50;"/>
            <Label text="• Track approval status in real-time" 
                   style="-fx-text-fill: #2c3e50;"/>
            <Label text="• You'll be notified when all approvals are complete" 
                   style="-fx-text-fill: #2c3e50;"/>
        </VBox>
    </VBox>

</VBox>